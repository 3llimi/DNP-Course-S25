# Instructions

1. Create a file named `server.py` in this directory
2. Write your code there. Then push updates to your repository.

    ```bash
    git add -A
    git commit -m "Add/Update server.py"
    git push
    ```

3. Monitor autograder results in `GitHub Actions` section.
4. Check for assignment updates or instructors feedback in `Pull requests` section.

> [!CAUTION]
>
> - Do NOT create/modify any files except `server.py`. Violations will be automatically detected.
> - Do NOT share your code with others or make it public. Plagiarism cases will also be autmatically detected.
